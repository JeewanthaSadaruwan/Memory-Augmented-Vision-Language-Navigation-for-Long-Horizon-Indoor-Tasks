"""Visualization helper functions placeholder."""


def save_frame(image, output_path: str) -> None:
    """Save one frame to disk (placeholder implementation)."""
    _ = image
    with open(output_path, "wb") as f:
        f.write(b"")
